%{
#include <stdio.h>
%}

%%
"//".*                          { printf("Single-line comment\n"); }

"/*"([^*]|\*+[^*/])*\*+"/"      { printf("Multi-line comment\n"); }

\n                              { /* ignore newline */ }

.                               { /* ignore other characters */ }
%%

int main()
{
    yylex();
    return 0;
}

int yywrap()
{
    return 1;
}
