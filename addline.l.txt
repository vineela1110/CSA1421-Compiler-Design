%{
#include <stdio.h>
int line_no = 1;
%}

%%
.*   {
        printf("Line %d: %s\n", line_no, yytext);
        line_no++;
     }
%%

int main() {
    yylex();
    return 0;
}

int yywrap() {
    return 1;
}
