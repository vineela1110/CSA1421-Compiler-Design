%{
#include <stdio.h>
%}

%%
"#include"[ \t]*"<"[a-zA-Z0-9_.]+">"   {
        printf("Header file: %s\n", yytext);
}

"#include"[ \t]*\"[a-zA-Z0-9_.]+\"     {
        printf("Header file: %s\n", yytext);
}

"#define"[ \t]+[a-zA-Z_][a-zA-Z0-9_]*  {
        printf("Macro definition: %s\n", yytext);
}

\n      { /* ignore newline */ }
.       { /* ignore other characters */ }
%%

int main()
{
    yylex();
    return 0;
}

int yywrap()
{
    return 1;
}
